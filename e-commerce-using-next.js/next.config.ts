import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['/shopping-cart-with-dollar-sign-5399ld.png'], // Add your image domains
  },
  reactStrictMode: true,
};

export default nextConfig;

